# Tiến độ triển khai

Cập nhật gần nhất: 2026-09-24 13:12 (Asia/Ho_Chi_Minh)

## Trạng thái hiện tại

- Snapshot revalidated lúc 13:12: PR #1 còn mở và mergeable; trước đợt docs này branch `codex/product-v1-completion` trỏ tới `1f99bb7709f356975637b241895a34bc50e9764f`, base `main` ở `07938bd2d0a9793baff86711b39c8d181c0121ca`. Backend workflow #68 pass trên docs refresh `1f99bb7`; workflow #67 pass trên README/docs/Compose update `f744033`. Code hardening `c5bb13e` có workflow #65 pass; docs snapshot `7b15093` có workflow #66 pass.
- Phase hiện tại: Phase 7 — hardening và bàn giao core pilot. Real-mode browser flow trên PostgreSQL 18.3/pgvector 0.8.2 gồm campaign/bài thủ công, media, approval, XLSX, metrics snapshot, dashboard readback và recommendation abstention. Kiểm tra bổ sung xác nhận tài khoản/campaign còn đọc được sau khi dừng rồi khởi động lại cả FastAPI và PostgreSQL. Readiness chịu tải nhẹ; database dump/restore counts khớp và archive/restore local object storage giữ nguyên SHA-256.
- Runtime: Celery `solo` phục hồi stale lease và đã chạy upload TXT/DOCX/PDF/XLSX/CSV qua FastAPI/PostgreSQL/Redis. DOCX paragraph+table lưu 2 knowledge chunks; PDF/XLSX/CSV mỗi loại lưu document chunk và knowledge chunk. Live DeepSeek adapter smoke hiện pass; việc trích xuất Brand Profile qua worker bằng provider thật chưa được kiểm chứng.
- Hosted CI backend run #62 pass trên parser code `c9fc10d`; run #65 pass trên hardening code `c5bb13e`; run #66 pass trên docs head `7b15093`; run #67 pass trên docs/Compose follow-up `f744033`; run #68 pass trên docs refresh `1f99bb7`. Local Python suite có **147 passed, 1 skipped** trên hardening implementation snapshot; full suite giữ live API marker opt-in nên skip đó vẫn có trong lượt chạy mặc định. Một lượt smoke riêng trên current PR code gọi model list và một generation tổng hợp: **1 passed**. 13 focused security/body-limit tests, OpenAPI check, compileall, `git diff --check`, Compose YAML, Dockerfile path và README link checks pass. Docker/Podman không có nên Compose boot chưa chạy; CI không biến container/runtime thành đã nghiệm thu.
- Mốc RAG: local FastEmbed multilingual E5 small pinned 384d đã chạy qua adapter; gate yêu cầu semantic margin hoặc lexical evidence mạnh. PostgreSQL migration legacy `vector(1536)` giữ dữ liệu cũ, nhận vector 384 và lọc theo model identity. Python suite **147 passed, 1 skipped** là lượt full suite mặc định; DeepSeek API smoke được chạy riêng và pass với model `deepseek-flash`.
- E5 calibration chạy model thật trên dữ liệu tổng hợp: 6/6 truy vấn đúng hit đúng nguồn hạng 1 và 0/8 truy vấn ngoài miền trả context. Cũ hơn, ranking holdout 12 truy vấn đạt Hit@1 75%, Hit@3 91.7%, MRR .850. Cả hai bộ đều nhỏ, không đại diện corpus SME.
- Checkout dùng cho task là `/private/tmp/agentic-v1-media`; worktree gốc `/Users/lethanh/agent` vẫn ở `main` (`648ff66`, ahead 1/behind 1) với staged/unstaged/untracked M1/M2/M3 changes. Không sửa, stage, stash hoặc commit các thay đổi đó. Trong worktree bàn giao có file untracked `:memory:.ses`; đã giữ nguyên và không stage.
- Chủ dự án đã chấp thuận gửi đoạn trích tài liệu, Brand Profile và strategy/topic/date của slot được chọn tới DeepSeek. Key đã được cấu hình trong `.env` bị Git ignore của isolated test checkout; smoke test lúc 13:00–13:02 xác nhận account liệt kê `deepseek-flash` và trả JSON đúng Pydantic schema với payload tổng hợp. Smoke không gửi tài liệu/campaign của workspace. Test không lưu token usage/latency vào report; chi phí thực vẫn unavailable. Full Brand Profile/content worker/browser flow chưa chạy với provider thật.
- Công việc mới trong phiên: hoàn tất password reset và lời mời thành viên end-to-end ở mức fixture/test. API có SMTP adapter tùy chọn, token reset một lần, thu hồi reset tokens và refresh sessions, vô hiệu access token theo password version, preview/accept invitation và resend xoay token; giao diện có mời/resend, trang tạo tài khoản từ link và đặt lại mật khẩu. Khi SMTP chưa cấu hình, lời mời trả link thủ công rõ ràng và reset password vẫn trả thông báo trung tính nhưng không giả nhận đã gửi.
- SMTP thật chưa được cấu hình hoặc gọi. `.env.example` chỉ khai báo các biến SMTP tùy chọn; không có secret nào được thêm vào repo. Email tests dùng fake SMTP và test local không gửi thư ra ngoài.
- Recheck lúc 09:04 trên source SHA `9d3dd6d`: Python **123 passed, 1 skipped**; frontend **45 Vitest passed**, typecheck, lint, production build pass; Playwright mock E2E **42/42** desktop/mobile pass. Real-mode Playwright **1/1** pass với SQLite migration 0001→0010, API thật và local storage: campaign → bài thủ công → upload ảnh → versioned approval → XLSX export/download. Các test không gọi DeepSeek, SMTP hoặc Meta.
- DeepSeek docs được kiểm tra lại ngày 2026-09-24: `deepseek-flash` hiện gọi V4.1-Flash; `deepseek-v4-pro` route về Flash từ 14/9 tới khi có V4.1-Pro; JSON mode dùng `response_format={"type":"json_object"}`. Giá peak hiện tại: $0.30/M input cache miss, $0.006/M cache hit, $1.20/M output. Smoke xác nhận `deepseek-flash` khả dụng cho account này; chưa có token usage/cost/latency đo và không coi scenario stress chi phí trong `docs/test-report.md` là OPEX thực.
- Meta docs: đã thử đọc trực tiếp Graph API Page Feed, Page Insights, permission reference và access-token guide ngày 2026-09-24; các trang trả HTTP 429. Lượt audit lúc 12:37 thử lại tìm kiếm chính thức và các URL `developers.facebook.com`; tìm kiếm chính thức không trả kết quả và mở trang tiếp tục trả HTTP 429. Search chỉ đưa ra mirror không thuộc Meta, nên không được dùng làm nguồn chuẩn. Version, permission, token, publish, metrics, limits và webhook hiện hành vẫn `VERIFY CURRENT META API`. Không có app/Page/token/App Review; connector tự động tiếp tục `BLOCKED_EXTERNAL`, fallback manual vẫn có trong UI.
- Docker/Podman/MinIO vẫn không có; PostgreSQL 18.3 test trên các port 55432/55433 và Redis 8.6.3 trên 56379 đều chạy trong cụm cô lập dưới `/private/tmp`, không chạm database dùng chung, và đã dừng sau test. SMTP variables chưa có; DeepSeek key chỉ có trong ignored local `.env`, chưa provision production secret store. Compose/Beat/MinIO, live SMTP, provider-backed Brand/content jobs, metrics từ Meta và deploy production chưa được nghiệm thu. Readiness latency đo được chỉ đại diện máy local; token usage/chi phí thực chưa có.
- Playwright config giờ build rồi chạy đúng standalone server `node .next/standalone/apps/web/server.js`, đồng thời copy `public/` và `.next/static/` vào output như Dockerfile production. Lượt đầu thử standalone thiếu hai thư mục này và E2E không render form; sau khi thêm bước chuẩn bị, full mock và real-mode E2E đều pass, không còn cảnh báo `next start`. Pytest có một LangGraph pending-deprecation warning; Vitest in cảnh báo cấu hình Vite native loader.

## Tiến độ theo subsystem

| Subsystem | Implementation | Verification | Evidence | Next action |
|---|---|---|---|---|
| Auth/workspace/membership | Session, workspace/tenant permissions, one-time password reset, invitation preview/accept/resend; optional SMTP delivery + manual link fallback | Full Python suite pass; account lifecycle integration tests cover SMTP success/failure, reset replay/session revocation, resend token rotation, invite acceptance; Playwright invite UI 2/2 desktop/mobile pass | `services/api/auth.py`, `services/api/workspaces.py`, `services/api/email.py`, `apps/web/src/app/invite`, `apps/web/src/app/reset-password` | Provision SMTP in deployment and test delivery/recovery against a controlled mailbox; repeat lifecycle on deployment PostgreSQL/Redis. |
| Upload/ingestion/jobs | Parser PDF/DOCX/XLSX/CSV/TXT, durable ingestion/recovery và server-generated object keys; preflight batch; parser version `m2-parser-v2` | TXT/DOCX/PDF/XLSX/CSV đã chạy qua FastAPI/PostgreSQL/Redis/Celery; 10 parser tests cover định dạng/locator/limits, gồm XLSX tại storage key không có suffix; preflight 415/413 không tạo object/row; reprocess dùng version hiện tại; suite 147 pass | `services/api/documents.py`, `services/ingestion/parsers.py`, `tests/test_ingestion_parsers.py`, `docs/test-report.md` | Worker process restart, MinIO và Compose còn mở. |
| Brand Profile | Extraction worker, source citations, revision/edit/confirm; DeepSeek server-side adapter | Fake-model API/worker tests pass; account model-list + synthetic structured-output smoke pass; real profile extraction worker path NOT_VERIFIED | `services/api/brand_profiles.py`, `services/worker/tasks.py`, `tests/test_deepseek_api_smoke.py` | Run provider-backed Brand Profile job with an isolated DB/queue and record source citations, usage and latency. |
| Retrieval | Tenant/source/version/locator filters; local multilingual E5; query/passage prefixes; confidence margin and lexical relevance gates | 30 focused tests pass; real E5 synthetic check 6/6 positive Hit@1 and 0/8 no-answer false contexts; PostgreSQL legacy-to-flexible vector migration and dimension-isolated retrieval pass | `services/agents/providers/fastembed.py`, `services/agents/knowledge/retrieval.py`, `scripts/evaluate_retrieval.py`, `docs/test-report.md` | Provision model cache, reprocess documents and evaluate against permitted SME corpus. |
| DeepSeek | Một Chat Completions JSON-mode adapter dùng worker factory; timeout/input/output limits; default `deepseek-flash` | Docs/model IDs hiện hành đã xem; fake-client pass; live model-list + structured JSON smoke 1/1 pass. Usage/latency chưa được ghi; estimated cost unavailable | `services/agents/providers/deepseek.py`, `services/worker/model_provider.py`, `tests/test_deepseek_api_smoke.py`, `docs/decisions.md` | Run provider-backed Brand Profile and campaign generation through API/worker; capture usage metadata and keep cost unavailable until measured/priced. |
| PostgreSQL migrations/API | Alembic 0001→0010, gồm media assets/approval hash và flexible embedding dimensions | PostgreSQL 18.3 + pgvector 0.8.2 migration; real-mode browser campaign/media/approval/export pass trên DB này; SQLite cũng pass | migration 0010, `tests/e2e/manual-workflows.real.spec.ts`, `docs/test-report.md` | Chạy Brand/content job và recovery trên PG/Redis với provider stub/live key; thử MinIO/S3. |
| Runtime/infrastructure | Compose khai báo API, worker, scheduler, PostgreSQL, Redis, object storage | Local API `/readyz`: 100/100 ở concurrency 10, p95 38.05 ms; khi Redis dừng trả 503 và hồi phục sau restart. Celery recovery đã có test cô lập trước đó. Compose, Beat, MinIO, process restart và prefork Linux chưa xác minh | `scripts/load_smoke.py`, `infra/`, `docker-compose.yml`, `docs/test-report.md` | Full Compose/MinIO, Beat và worker prefork Linux; load test business endpoint trên cấu hình staging. |
| Campaign/content slots | Brief, strategy, slots, durable generation/revise, version guard | API/worker fixtures và desktop/mobile mock E2E pass; live adapter smoke pass with synthetic JSON; actual job/browser generation NOT_VERIFIED | `services/api/campaign_workflows.py`, `services/worker/content_tasks.py` | Run a live generated slot through API/worker, persist draft/version and verify approval gate with isolated data. |
| Media assets | JPEG/PNG/WebP upload, tenant-scoped preview/download, attach/detach theo version, export reference | Python integration; 14/14 desktop/mobile MSW E2E; real API/browser E2E pass trên SQLite và PostgreSQL, xác nhận version 2, media SHA, approval hash và XLSX download | `services/api/media.py`, post editor, `tests/e2e/manual-workflows.real.spec.ts`, migration 0009 | DONE cho manual media workflow; Meta image publishing thuộc META-001. |
| Approval/export | Approval gắn exact version và SHA-256 của nội dung/media; CSV/XLSX có media filename/hash/path | API test và full frontend build pass; manual export/download browser evidence đã có | `services/api/content_integrity.py`, campaign workflow, `docs/test-report.md` | Publisher tương lai phải so hash trước khi gửi; Meta connector chưa có. |
| Meta | One-Page connector: server token, read-only identity/feed verification, owner-only approved text/one-image publishing, ambiguity reconciliation, resumable history import and Analytics table | MockTransport client **17 passed**; web typecheck and OpenAPI/codegen checks pass; no live Graph request | `services/api/meta.py`, `services/api/meta_client.py`, `services/worker/meta_tasks.py`, migration 0011, Settings/Publishing/Analytics, `docs/runbook.md` | Set Page token in backend secret store; verify Graph version, feed fields and write permissions against the real app/Page. Never enable real publishing before owner selects an approved post. |
| Metrics/dashboard | Manual snapshots, null-aware KPI, coverage/freshness, grouping | SQLite API tests; PostgreSQL real-mode browser test saves a snapshot and verifies dashboard reach 250, sample size 1 and coverage 1 | `services/api/analytics.py`, analytics UI | Nhập nguồn số liệu thật; Meta sync còn mở. |
| Recommendation | Evidence/abstain, feedback, apply draft, owner accept/version guard, outcome windows | API tests and desktop/mobile mock flow pass; real-mode PostgreSQL browser flow confirms abstention with one observation | recommendation routes/UI, migration 0006–0007 | Kiểm chứng với dữ liệu pilot; không diễn giải outcome như quan hệ nhân quả. |
| Frontend | Auth/password reset/invites, brand, campaigns, media editor, analytics, publishing guidance | Typecheck, **45 Vitest**, lint/build; standalone production server: **42/42** mock Playwright desktop/mobile, **1/1** SQLite real flow and **1/1** PostgreSQL real flow including metrics/dashboard/abstention | `apps/web`, `tests/e2e/slice1-brand.spec.ts`, `tests/e2e/manual-workflows.real.spec.ts` | SMTP delivery and real-mode AI generation/Brand Profile browser path need configured SMTP and DeepSeek secret. |
| Security/operations | CSRF, tenant checks, rate limits, upload validation, approval hash, reset-token/session lifecycle, SMTP secret handling | **147 pytest passed/1 live-DeepSeek skip**; host allowlist, trusted proxy IP/CIDR, aggregate body limit and production DeepSeek HTTPS tests pass; fake SMTP, reset replay/session revocation, local PostgreSQL dump/restore and object checksum, readiness/Redis outage smoke | `docs/security-review.md`, `docs/test-report.md`, `scripts/load_smoke.py` | Real SMTP, Celery worker restart, Compose/MinIO deployment, proxy/edge, scheduled backup, business-route load and full operational review remain open. |

## Blockers và ảnh hưởng

- **AI-001 — DeepSeek live validation:** user đã chấp thuận data flow cho tài liệu, Brand Profile và slot strategy/topic/date; adapter smoke 1/1 pass với model-list + structured JSON tổng hợp. Key chỉ có trong ignored local `.env`, chưa provision production. Full Brand Profile/content job qua worker/browser chưa chạy; smoke không lưu usage/latency, cost unavailable. Bước tiếp theo là chạy vertical slice provider-backed với database/queue cô lập.
- **MAIL-001 — SMTP delivery:** password reset/invitation email và manual invitation fallback đã triển khai; SMTP behavior được test bằng fake client. Chưa có `SMTP_HOST`/credentials hoặc controlled mailbox trong môi trường, nên chưa xác minh delivery, TLS handshake, bounce hoặc SPF/DKIM. Các chức năng email chưa được tuyên bố live-ready.
- **RUN-001 / OPS-001 — full runtime:** upload TXT, DOCX, PDF, XLSX và CSV đã chạy qua PostgreSQL 18.3/Redis 8.6.3/Celery 5.6.3 `solo`; từng tài liệu đạt extraction và knowledge `ready`. Beat, worker process restart, Compose, MinIO, prefork Linux, backup/deployment restore còn mở.
- **LOAD-001 — production capacity/cost:** readiness-only local smoke đã chạy 100 request ở concurrency 10; không đo campaign/write path, job throughput, production hardware hoặc usage thật. Stress scenario giá DeepSeek được ghi trong test report nhưng không phải hóa đơn/dự báo OPEX; chi phí hosting/mail/storage chưa báo giá.
- **META-001 — external access:** code pilot one-Page đã nối nhưng chưa có Meta token trong môi trường backend, và chưa xác minh quyền/Graph fields với Page thật. Lịch sử Page đọc theo từng đợt tối đa 500 bài; publish từng bài yêu cầu owner xác nhận. Meta developer pages trả HTTP 429 trong audit, nên version/permissions/metric availability vẫn cần live validation.
- **RAG-001 — semantic embedding:** local provider runtime pass với pinned E5 weights, synthetic gate check đạt 6/6 positive Hit@1 và 0/8 no-answer context. Chưa có corpus SME để nghiệm thu quality; cần cache weights trên deployment, reprocess tài liệu cũ và đánh giá với corpus/query được pilot cho phép.

## Bằng chứng mới nhất

- Python 3.11.16 interpreter: full suite **147 passed, 1 skipped**; parser + Brand Profile integration focused snapshot at `c9fc10d` had **22 passed**. Lượt full-suite này chạy trước khi key được cấu hình và bỏ qua live smoke opt-in; lượt adapter smoke riêng sau đó pass.
- Frontend: `npm test` — **45 passed**; typecheck, ESLint và production `next build` pass. Playwright standalone production server: **42 mock tests pass** desktop/mobile và **1 real-mode SQLite browser test pass**; invite create/resend/accept nằm trong 42 tests.
- Browser: `npm run test:e2e -- slice2-campaign.spec.ts --workers=1` — **14 passed** (7 desktop + 7 mobile) với MSW. `manual-workflows.real.spec.ts` — **1 pass trên SQLite** và PostgreSQL real mode. PostgreSQL flow đăng nhập, campaign/post, ảnh, versioned approval, XLSX, lưu metrics snapshot, kiểm tra dashboard qua API (reach 250, n=1, coverage 1) và xác nhận recommendation abstain. Không gọi DeepSeek/Meta.
- Persistence: disposable PostgreSQL 18.3 + pgvector 0.8.2, FastAPI thật: tạo tài khoản/workspace/campaign, clean-stop API và DB, khởi động lại cả hai, login lại và đọc đúng campaign qua API. **HTTP 200 sau restart; ID/tên campaign được giữ nguyên.** Không chạy worker/Redis/MinIO trong phép thử này.
- Load/readiness: `scripts/load_smoke.py` trên PostgreSQL 18.3/pgvector 0.8.2, Redis 8.6.3 và local storage: 100/100 ready, concurrency 10, p95 38.05 ms; Redis outage trả 503 và tự hồi phục sau restart. Chỉ là smoke trên máy local.
- Database/runtime: PostgreSQL 18.3/pgvector 0.8.2 migration 0001→0010, legacy `vector(1536)` preservation, vector 384 insertion/retrieval, API/browser manual flow, pg_dump/restore counts và local storage checksum roundtrip pass. Runtime recovery và upload TXT/DOCX/PDF/XLSX/CSV dùng PostgreSQL 18.3, Redis 8.6.3, Celery 5.6.3 `solo`; các định dạng đã lưu normalized/document chunks và knowledge chunks; missing key làm profile step `ai_not_configured` mà không gọi model. Upload preflight tránh object/row một phần với batch unsupported/oversized; `m2-parser-v2` hiện hành và reprocess áp dụng version mới. Beat, worker restart, Compose, MinIO và prefork Linux vẫn mở.
- Worker routing/loop tests, OpenAPI `--check`, Python compileall, Compose YAML parse và `git diff --check` pass. Chi tiết môi trường/lệnh/giới hạn ở [test-report.md](test-report.md).
- RAG integration baseline at `837d933`: focused tests **26 passed**; full Python suite **112 passed, 1 skipped** (live DeepSeek). SQLite migration 0001→0010 pass. PostgreSQL 18.3/pgvector 0.8.2 migration từ `vector(1536)` giữ vector cũ, insert/retrieve 384 chiều pass, downgrade guard chặn mất dữ liệu.
- Retrieval hardening at `5a09c76`: **30 focused tests**, full backend **116 passed, 1 skipped**; local model run `scripts/evaluate_retrieval.py --cache-dir /private/tmp/agentic-e5-provider-check --local-files-only` returned **6/6 positive Hit@1, 0/8 no-answer false contexts**. Worker recovery snapshot at `380ac6c` had **118 passed, 1 skipped** before later account-lifecycle, parser and security changes.

## Việc tiếp theo

1. Provision DeepSeek key trong production secret store và SMTP credentials trong deployment; test mailbox. Model-list/JSON smoke đã pass với key cục bộ; tiếp theo chạy Brand→content worker/browser path thật và ghi token usage, latency, cost.
2. Chạy Docker Compose/MinIO và Celery Beat trên Linux; kiểm thử ingestion, worker restart/recovery, backup/restore, prefork và load lên campaign/metrics endpoints.
3. Dùng model cache/corpus SME được phép để kiểm tra retrieval; xác minh Meta docs/quyền/app khi có credential, và nghiệm thu duplicate-safe publisher. Manual publishing tiếp tục là fallback.

Không có URL service nào đang chạy. Hướng dẫn chạy và các giới hạn vận hành nằm ở [runbook.md](runbook.md); kế hoạch/phạm vi ở [implementation-plan.md](implementation-plan.md); kiểm thử ở [test-report.md](test-report.md).
