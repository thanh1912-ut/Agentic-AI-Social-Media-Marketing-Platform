# Task board — sản phẩm v1

Trạng thái chỉ đổi sang DONE khi implementation đã tích hợp và có evidence. Dùng `BLOCKED_EXTERNAL` khi thiếu quyền/dịch vụ bên ngoài; `VERIFY CURRENT META API` giữ rõ nội dung cần kiểm tra. Không dùng phần trăm thay evidence. Scope hiện tại là core pilot nêu trong `implementation-plan.md`.

| ID | Phase | Công việc | Priority | Status | Dependency | Acceptance criteria | File/module liên quan | Test/evidence | Blocker |
|---|---|---|---|---|---|---|---|---|---|
| SETUP-001 | 0 | Audit remote/local, giữ snapshot M1/M2/M3 | P0 | DONE | Repo access | Branch/base và staged/unstaged/untracked phân biệt; source checkout còn nguyên | Git metadata, worktree | `origin/main=07938bd`; local/main tree hash trùng | — |
| DOCS-001 | 0 | Tạo sáu tài liệu vận hành/tiến độ | P0 | DONE | Audit | Plan, progress, task board, test report, decisions, runbook cùng phản ánh trạng thái | `docs/*.md` | Đã đối chiếu với test thực và blocker | — |
| ENV-001 | 1 | Cài backend dev dependencies từ pyproject | P0 | DONE | Python 3.11 | `python -m pytest` import và chạy; phiên bản được ghi | `pyproject.toml` | Python 3.11.16; full pytest pass | — |
| ENV-002 | 1 | Cài frontend dependencies theo npm lock | P0 | DONE | Node >=22 | Clean npm install từ lockfile; audit không còn advisory | `package-lock.json`, `package.json` | `npm ci --ignore-scripts`; `npm audit` 0 vulnerability | Npm scripts cần kiểm tra riêng nếu chạy MSW/E2E |
| DB-001 | 1 | Chạy migrations trên database test PostgreSQL | P0 | DONE | ENV-001, isolated test DB | Alembic `head`, schema/index/pgvector đúng; không ghi production | `database/migrations/**`, `alembic.ini` | PostgreSQL 18.3/pgvector 0.8.2: migration 0001→0010 pass; kiểm thử schema deployed cũ `vector(1536)`, giữ lại vector cũ, insert vector 384 và truy vấn tách theo model version. Downgrade an toàn từ chối khi còn vector 384. SQLite 0001→0010 pass. PostgreSQL API/worker runtime còn là RUN-001. | — |
| RUN-001 | 1 | Khởi động API/worker/scheduler/storage và health checks | P0 | IN_PROGRESS | DB-001, Redis, storage | `/healthz`, `/readyz`; worker consume; restart giữ state | Compose, `services/api`, `services/worker` | PostgreSQL 18.3 + pgvector 0.8.2 migration 0001→0012; real-mode API/browser flows; account/campaign readback after API+DB restart; Celery `solo` recovery/upload; API ASGI dispatched actual `market_research_task` to Redis queue `agent`; Beat one-tick recovery enqueued a scheduled cycle and worker persisted report/evidence; docs/knowledge ready, no-key AI path explicit | Docker/Podman/Compose, MinIO, worker process restart and prefork Linux; API acceptance used in-process ASGI and scheduler tested one tick only |
| API-001 | 2 | Auth/workspace, CSRF/session lifecycle | P0 | IN_PROGRESS | ENV-001 | Login/logout/refresh/invite/reset; permission checks mọi workspace request | `services/api/auth.py`, `workspaces.py` | 147-test Python suite covers password reset, invite preview/accept/resend, token replay/rotation, session revoke; invitation UI E2E pass desktop/mobile; real API login and campaign readback pass after FastAPI/PostgreSQL restart | SMTP and deployment PostgreSQL/Redis lifecycle still need runtime acceptance |
| MAIL-001 | 2 | Optional SMTP email for password reset and member invitations | P1 | IN_PROGRESS | API-001, WEB-001 | One-time reset links; invite/resend links; neutral forgot response; explicit manual fallback; TLS and secrets server-side | `services/api/email.py`, `services/api/config.py`, `apps/web/src/app/forgot-password`, `reset-password`, `invite`, workspace settings | Fake SMTP STARTTLS/login test; sanitized failure; password-reset/invite API integration; Playwright invitation flow 2/2 | SMTP credentials and controlled mailbox are not configured; live delivery, bounce, SPF/DKIM remain unverified |
| KNOW-001 | 2 | Upload, parser, durable ingestion/recovery | P0 | IN_PROGRESS | DB-001, RUN-001 | PDF text/DOCX/XLSX/CSV/TXT; invalid/oversized; retries/restart; per-file status | `services/api/documents.py`, `services/ingestion`, `services/worker/tasks.py` | PostgreSQL/Redis/Celery `solo` runtime smoke pass cho TXT/DOCX/PDF/XLSX/CSV; PDF/XLSX/CSV mỗi loại tạo document chunk và knowledge chunk; XLSX extensionless storage key có regression test; 10 parser tests cover locators, corruption/empty/scanned PDF và bounds; preflight 415/413 leaves no object/row; reprocess stores current parser version | Worker process restart, MinIO và Compose chưa kiểm chứng |
| AI-001 | 2 | DeepSeek adapter JSON output và lỗi chuẩn hóa | P0 | IN_PROGRESS | ENV-001 | JSON schema prompt/validation, empty/truncated/error, tối đa 1 repair; không lộ secret | `services/agents/providers/deepseek.py`, provider tests | Fake-client tests pass; worker wired; opt-in live smoke **1 passed**: `deepseek-flash` listed for this account and one synthetic structured JSON generation validated | User approved data flow; full API/worker Brand Profile and content generation remain unverified. Live usage/latency were not captured; cost stays unavailable. Key is local only, not production secret-store provisioning |
| RAG-001 | 2 | Retrieval tenant/source/version/locator/relevance | P0 | IN_PROGRESS | KNOW-001 | Active tenant sources only; dedup metadata; low relevance abstains; locator xuyên suốt | `services/ingestion/knowledge_store.py`, `services/agents/providers/fastembed.py`, agents knowledge | Local multilingual E5 384d adapter and dimension-versioned pgvector migration; confidence margin + stopword-aware lexical rescue; full suite 147 passed/1 opt-in live marker skipped by default; separate live DeepSeek smoke 1 passed; real-model synthetic gate check: 6/6 positive Hit@1, 0/8 no-answer false contexts | SME relevance acceptance remains open; provision model cache, reindex existing sources and test with an approved SME corpus/query set |
| BRAND-001 | 2 | Upload → Brand Profile → source → edit/confirm | P0 | IN_PROGRESS | KNOW-001, AI-001, RAG-001 | Profile draft có citations thật; revision conflict 409; confirmation bền qua restart | `services/api/brand_profiles.py`, worker/agents | Fake-agent fixture tests pass; PostgreSQL/Redis worker ingestion persisted the source; missing-key path records `ai_not_configured`; separate adapter smoke passes, but real profile extraction has not run | Run a provider-backed Brand Profile worker job using isolated data; verify citations, persisted revision and confirmation; Compose/MinIO remains open |
| WEB-001 | 2 | Auth/docs/brand UI real mode | P0 | IN_PROGRESS | API-001, BRAND-001 | `NEXT_PUBLIC_USE_MOCKS=0`; API lỗi hiện rõ, không fallback; persistence qua reload | `apps/web/src/**` | Source `9d3dd6d`: typecheck/lint/build pass, 45 Vitest, 42 standalone mock Playwright pass desktop/mobile, 1 real SQLite campaign/media/approval/export flow pass | SMTP delivery and AI-generated content/Brand extraction real-mode acceptance remain |
| CAMP-001 | 3 | Campaign/brief domain, schema và API | P0 | DONE | BRAND-001 | Create/list/detail/edit tenant-scoped; brief validation; optimistic version và stale update trả 409; OpenAPI/UI đồng bộ | `database/models.py`, migration 0004, `services/api/campaign_workflows.py`, campaign detail UI, contracts | SQLite API test xác nhận edit tăng version và stale version trả 409; desktop/mobile Playwright xác nhận sửa brief hiển thị phiên bản mới; OpenAPI check | — |
| PLAN-001 | 3 | Campaign strategy và content slots có thể chỉnh sửa | P0 | DONE | CAMP-001 | Lưu strategy/cadence/slot dates, pillar, format, topic; sinh bài từ slot; version conflict; UI hiển thị lịch thực tế | `database/models.py`, migration 0008, campaign API/worker/UI, OpenAPI/contracts | API/worker fixtures cover slot reservation, same-key idempotency, duplicate protection, retry/cancel, slot→draft link and approved DeepSeek payload; desktop/mobile Playwright verifies edit→generate route | Live DeepSeek and full PostgreSQL/Redis/MinIO runtime remain separate acceptance items |
| MEDIA-001 | 2–3 | Upload ảnh làm asset và gắn vào post version | P1 | DONE | KNOW-001, VERSION-001 | Validate ảnh, tenant-scope metadata/preview, attach/detach theo version; approval hash bao phủ media; export/manual publish có media reference | `services/api/media.py`, migration 0009, post API/editor, OpenAPI/export | Python integration test covers validation/tenant/version/hash; 14 desktop/mobile MSW E2E pass; real-mode UI+FastAPI E2E pass trên SQLite và PostgreSQL 18, gồm gắn ảnh→version 2→approval→export/download; API readback xác nhận asset SHA và approval hash | Meta image auto-publish remains META-001 |
| CONTENT-001 | 3 | Tạo content generation job bằng DeepSeek | P0 | IN_PROGRESS | CAMP-001, BRAND-001 | Durable/idempotent job; confirmed profile + relevant tenant context; exact citations; immutable draft; no auto-approval/publish | agents, worker, prompts, API, frontend | SQLite fixture API+worker integration verifies idempotency, citations, draft/version; live adapter smoke passes with synthetic JSON; actual provider-backed content job/browser path pending | Key is configured only in isolated local `.env`; verify the complete API/worker/approval flow on disposable PostgreSQL/Redis before production secret/runtime setup |
| CONTENT-002 | 3 | AI revise bài thành immutable PostVersion | P0 | DONE | CONTENT-001, VERSION-001 | Scope rõ; active tenant context + exact citations khi đổi caption; expected-version guard; giữ field ngoài scope; approved post quay lại draft cần duyệt lại; không publish | `services/api/campaign_workflows.py`, `services/worker/content_tasks.py`, post editor, OpenAPI | SQLite API+worker fixture test covers job/idempotency, exact version, scope preservation, citation validation, `ai_revised`, reapproval; retry/recovery; Playwright MSW 8 pass desktop/mobile | Live revise job/browser path and runtime usage/token/latency evidence remain pending; cost estimate remains unavailable |
| VERSION-001 | 3 | Post versions, edit/revise, concurrency | P0 | DONE | CAMP-001 | Mỗi sửa tạo immutable version; stale expected version trả 409 | models, API posts, frontend | API tests: manual edits and AI revise create new versions; old versions remain readable; stale version conflict | — |
| APPROVAL-001 | 3 | Approval gắn exact immutable version | P0 | DONE | VERSION-001 | Edit sau duyệt cần duyệt lại; decision chỉ nhận exact pending/current version | API/approvals | API test: stale version 409, exact version approval, edit sets reapproval | Publishing guard chưa có |
| EXPORT-001 | 3 | CSV/XLSX content calendar và download | P0 | DONE | CAMP-001, VERSION-001 | Download file thật; formula-safe cells; export không bị trình bày như publish | `services/api/campaign_workflows.py`, object storage | CSV/XLSX API download and idempotency test | S3/MinIO runtime chưa xác minh |
| META-001 | 4 | One-Page Meta pilot: verified read, approved text/image publish, reconcile, historical post metrics | P1 | IN_PROGRESS | APPROVAL-001; user-owned Meta App/Page/token | Owner configures backend secret, verifies Page identity/feed read, sends only the exact approved version, sees historical Page posts, and reconciles ambiguous sends without blind retry | `services/api/meta*`, `services/worker/meta_tasks.py`, migration 0011, OpenAPI, publishing/settings/analytics UI, runbook | MetaGraphClient MockTransport **17 passed**; frontend typecheck, OpenAPI export/check, generated TypeScript and diff check pass. No live Graph call; app/Page permission and publication acceptance remain unverified. | User must place Page token in backend secret store and verify current Graph version/permissions on the real Page; no token is stored in this worktree |
| METRICS-001 | 5 | Manual metric import và snapshots | P1 | DONE | CAMP-001; META optional | Mapping/validation/dedup; source/time/post-age; freshness; null khác zero | models, migration 0005, `services/api/analytics.py` | SQLite API import, duplicate, tenant isolation tests; PostgreSQL real-mode UI saves a metrics snapshot; migration 0001→0007 | Meta auto-sync optional |
| DASH-001 | 5 | Dashboard campaign/post/pillar/format | P1 | DONE | METRICS-001 | KPI tính trong code; post-age semantics; coverage/freshness hiển thị | API analytics, `apps/web` analytics page | API report tests, production build, desktop/mobile mock E2E; real-mode PostgreSQL browser E2E verifies persisted reach 250, n=1, coverage 1 | Không xác minh Meta ingestion hay production |
| REC-001 | 6 | Evidence-backed recommendation loop | P1 | DONE | DASH-001 | Evidence IDs, hypothesis/action/metric/threshold/confidence; abstain khi ít data; feedback lưu; apply tạo revision; owner accept/discard với version guard | `services/api/analytics.py`, migration 0006, analytics UI | API tests cover idempotent save, feedback, no mutation before accept, accept/version bump, stale 409; desktop/mobile E2E pass; PostgreSQL real-mode UI confirms abstention at n=1 | — |
| REC-002 | 6 | Ghi nhận kết quả thử nghiệm sau khi chạy | P2 | DONE | REC-001, METRICS-001 | Gắn window/metric thực tế với brief revision; so sánh cùng tuổi bài, lưu outcome và hạn chế suy luận nhân quả | `database/models.py`, migration 0007, `services/api/analytics.py`, analytics UI/OpenAPI | SQLite API test: accepted revision, cohort metrics, evidence/snapshot IDs, change, limitations, idempotency và readback; Playwright desktop/mobile ghi outcome qua MSW | Phải có snapshots baseline/follow-up cùng nguồn và tuổi bài; số liệu mô tả, không chứng minh nhân quả |
| SEC-001 | 7 | Tenant/security/secret review | P0 | IN_PROGRESS | API-001, all write routes | Cross-tenant access denied; secrets absent frontend/logs; upload limits/CSRF/rate limits reviewed | API/auth/storage/logging | 147 Python tests; tenant API checks; production rejects weak JWT secret/disables docs/disables rate limits; explicit CORS; runtime-config escaping; refresh/logout CSRF; upload traversal; Redis limits on auth/upload/generation; reset replay/session revocation; SMTP subject/header sanitization; host allowlist/trusted proxy/body limit/DeepSeek HTTPS checks; npm audit clean | Deployment proxy IPs, edge controls, Redis/Compose runtime and full route review remain; see `docs/security-review.md` |
| OPS-001 | 7 | Backup/restore, job recovery, runbook | P1 | IN_PROGRESS | DB-001, RUN-001 | Backup phục hồi test DB; queued/lease recovery idempotent; rollback steps verified | DB/worker/runbook | PostgreSQL `pg_dump -Fc`/`pg_restore` giữ nguyên counts; local object archive checksum roundtrip 2 objects; stale lease recovery; upload TXT/DOCX/PDF/XLSX/CSV through API/PostgreSQL/Redis/Celery; per-format document/knowledge chunks persist; parser/unit and preflight integration coverage; fresh API-created account/campaign readback survives FastAPI + PostgreSQL clean restart | Worker process restart, deployment backup schedule, object storage production recovery và rollback vẫn chưa nghiệm thu |
| E2E-001 | 7 | E2E real-mode acceptance | P0 | IN_PROGRESS | WEB-001, phases 2–6 | Fresh account: login→brand→content→approval→export→metrics/reco path; no MSW | `apps/web`, `tests/e2e/manual-workflows.real.spec.ts`, API, docs test report | PostgreSQL 18.3/pgvector 0.8.2 real browser flow **1 passed**: campaign→manual post→media→approval→XLSX→metrics snapshot→dashboard API readback→recommendation abstains at n=1; 42 standalone mock E2E pass; separate live DeepSeek adapter smoke **1 passed** | Full provider-backed Brand Profile/content browser flow, SMTP, Compose/MinIO and Meta access remain open; local key is not provisioned to production |
| LOAD-001 | 7 | Basic load/latency/cost readiness | P2 | IN_PROGRESS | Working core | Record concurrency, latency, failure/retry; cost source/date; compare monthly budget | `scripts/load_smoke.py`, docs/test-report.md | Isolated PostgreSQL 18.3 + Redis 8.6.3 + local storage: `/readyz` 100 requests/concurrency 10, all 200, p95 38.05 ms; Redis outage returned 503 and readiness recovered after restart. Current DeepSeek peak price source + stress scenario recorded; no live token/usage data | Local readiness only, not campaign throughput or production capacity; real workload, live provider spend and hosting/mail/storage costs remain unmeasured |
| MR-001 | 8 | Nhiều nhóm ngành/khu vực và kết nối nhiều Fanpage theo nhóm | P1 | IN_PROGRESS | Auth/workspace; META-001 | Owner tạo nhóm, kết nối tối đa 5 Page/workspace bằng Page ID + token được mã hóa, quản lý tenant-safe; trạng thái crawl hiển thị trong UI | `services/api/market_research.py`, `database/migrations/0012_market_research_and_page_groups.py`, `apps/web/src/app/w/[workspaceId]/fanpages/page.tsx` | SQLite API fixtures, fake Meta Graph client, token-encryption and tenant checks pass; UI Vitest/typecheck/lint pass. Chưa chạy Page thật hoặc PostgreSQL/Redis | Backend runtime và quyền Meta/Page token thật chưa được cấu hình trong worktree |
| MR-002 | 8 | Lưu URL website/Page và thu thập thủ công/định kỳ mỗi 12 giờ | P1 | IN_PROGRESS | MR-001, RUN-001 | Người dùng lưu URL một lần; website công khai được crawl bounded/robots-aware; Page chỉ đọc khi Meta cấp quyền; job có snapshot, trạng thái và lịch chạy | `services/research/web_crawler.py`, `services/worker/research_tasks.py`, `services/worker/celery_app.py` | `taphoammo.vn` persisted crawl via API/inline on PG: 4 evidence/observations/raw SHA-256 snapshots. Separate actual API→Redis `agent`→Celery `solo` run and Beat recovery tick on Example Domain: manual and scheduled cycles/jobs `succeeded`; 2 reports, 2 observations/snapshots; next due +12h | Website queue/scheduler path verified. Live Page-source collection requires Meta access; full Uvicorn/Compose, worker restart and Linux prefork remain unverified |
| MR-003 | 8 | Thu thập views, interactions, comments, followers và biến động | P1 | IN_PROGRESS | MR-001, MR-002 | Lưu null khi Meta không cấp metric; snapshots có thời gian; UI chỉ so sánh bài cùng nguồn, không cộng follower cấp Page hoặc suy luận nhân quả | `services/api/meta_client.py`, `services/worker/research_tasks.py`, market research UI | Fixture tests cover `post_media_view`, `followers_count`, comments, metric coverage và delta giữa hai lần snapshot; UI supports manual views/followers. Chưa xác minh metric bằng Page thật | Cần Page token/App Review hiện hành và Page thật đủ quyền |
| MR-004 | 8 | Thu thập Facebook Group tự động mỗi 12 giờ | P1 | BLOCKED_EXTERNAL | MR-001, Meta product access | Chỉ tự động khi Meta cung cấp API/quyền hợp lệ; hiện hỗ trợ lưu link và nhập dữ liệu người dùng được phép dùng | `services/api/market_research.py`, market research UI, `docs/meta-feasibility-spike.md` | Manual import API/UI/fixtures có; không scrape group hoặc tự động hóa trình duyệt. Meta Graph API v19 loại Groups API; Content Library bị giới hạn cho tổ chức nghiên cứu đủ điều kiện | Meta không cung cấp quyền Groups API cho connector SME thương mại hiện tại |
| MR-005 | 8 | Báo cáo xu hướng bằng DeepSeek và tạo campaign nháp có nguồn | P1 | IN_PROGRESS | MR-002, MR-003, AI-001 | Báo cáo giới hạn evidence, nêu coverage và nguồn; gợi ý được chọn mới tạo draft; evidence bên ngoài không xác thực claim thương hiệu | `services/worker/research_tasks.py`, market report/draft API, `apps/web/src/app/w/[workspaceId]/fanpages/page.tsx` | Schema/provider fixtures, evidence selection, PII redaction and draft path tests pass. Persisted `taphoammo.vn` crawl created a report with `analysis_status=deepseek_not_configured`; no model call and no campaign draft in this run | No DeepSeek key in current API/worker process; live market analysis and evidence-to-draft real path remain unverified |
| SCOPE-001 | 0 | Ghi deferred scope | P2 | DONE | Product scope | Video/AI media/Google OAuth/ads/multi-platform/K8s etc. không bị trình bày là v1 done | `implementation-plan.md` | Scope section | — |
