# npm workspaces tại gốc repo.
# Giữ node_modules "nested" để `npm ci --prefix apps/web` và `next build`
# bên trong apps/web chạy giống nhau (dùng cho Dockerfile.web của M2).
install-strategy=nested
